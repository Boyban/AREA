version: '3'
services:
        client_web:
                container_name: client_web
                image: web
                build: .
                ports:
                        - "8081:8081"
                depends_on:
                        - server
        mobile_user:
                container_name: mobile_user
                image: mobile
        server:
                container_name: server
                image: server
                restart: always
                build: .
                ports:
                        - "8080:8080"
                links:
                        - mongo
        mongo:
                container_name: mongo
                image: mongo
                volumes:
                        - ./data:/data/db
                ports:
                        - "27017:27017"
